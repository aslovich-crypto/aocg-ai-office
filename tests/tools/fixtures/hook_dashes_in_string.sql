-- ОПОРА ДЛЯ ЗАМЕРА hook_probe.py. НЕ ЗАПУСКАТЬ — это данные.
-- Случай B16 — обратный к A18. Двойное тире стоит ВНУТРИ строкового
-- литерала, комментарием не является, и следом на ТОЙ ЖЕ строке идёт
-- настоящее удаление. Наивное снятие «от -- до конца строки» срезало бы
-- его вместе с литералом, и сторож пропустил бы удаление.
UPDATE notes SET body = 'см. -- откат ниже'; DELETE FROM user_consents;
