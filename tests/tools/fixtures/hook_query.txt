-- ОПОРА ДЛЯ ЗАМЕРА hook_probe.py. НЕ ЗАПУСКАТЬ.
-- Случай B7: запрос лежит в .txt и передаётся в psql через -f.
-- Расширение «незнакомое» намеренно: закрывать обход списком расширений
-- бессмысленно, сторож обязан смотреть на роль аргумента.
DELETE FROM user_consents WHERE user_id = 'local_user';
