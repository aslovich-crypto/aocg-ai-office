"""Приложение «Финансы», модуль «Бюджет проекта» (ТЗ AOCG-FIN-PBM-001 v0.5).

Границы пакета — в CLAUDE.md, раздел «Приложение „Финансы“»: код Финансов
живёт только здесь, таблицы — с префиксом `fin_*`, код и таблицы Примы
не трогаются. Разрешённых касаний вне пакета ровно два: `include_router`
в `app/main.py` и один вызов `init_finance_schema(conn)` в `init_db()`.
"""
