"""Выгрузки наружу: сборка файлов из данных Примы.

Пакет намеренно НЕ ходит в базу и ничего не знает про HTTP: сюда приходят
готовые словари, отсюда уходят байты. Роутер берёт данные, зовёт сборщик,
отдаёт файл — раскладка репозитория («бизнес-логика отдельными модулями,
не внутри роутеров») требует ровно этого.
"""
