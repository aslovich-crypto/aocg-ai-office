# Конфигурация инструментов проекта (бэкенд aocg-ai-office).

[tool.ruff.lint.per-file-ignores]
# Тесты: поздние импорты (E402) — для изоляции/ленивой загрузки; lambda-присваивания
# (E731) — компактные локальные хелперы в тест-кейсах. Узаконено осознанно.
"tests/*" = ["E402", "E731"]
